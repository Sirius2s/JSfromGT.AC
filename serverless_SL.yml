# serverless.yml

#组件信息
component: scf # (必选) 组件名称，在该实例中为scf
name: SLscript # (必选) 组件实例名称。

#组件参数配置
inputs:
  name: scf-${name} # 云函数名称，默认为 ${name}-${stage}-${app}
  enableRoleAuth: true # 默认会尝试创建 SCF_QcsRole 角色，如果不需要配置成 false 即可
  src: ./
  handler: index.main_handler #入口
  runtime: Nodejs12.16 # 运行环境 默认 Nodejs10.15
  region: ap-chengdu # 函数所在区域
  description: This is a function in ${app} application.
  memorySize: 128 # 内存大小，单位MB
  timeout: 900 # 超时时间，单位秒
  events: # 触发器
    - timer: # 京东极速版红包 至2021-3-3
        parameters:
          name: jd_speed_redpocke
          cronExpression: "0 0 * * *"
          enable: true
          argument: jd_speed_redpocke
    - timer: # 粉丝互动
        parameters:
          name: jd_fanslove
          cronExpression: "8 10 * * *"
          enable: true
          argument: jd_fanslove
    - timer: # 百变大咖秀 4.22 end
        parameters:
          name: jd_entertainment
          cronExpression: "10 10,11 * * *"
          enable: true
          argument: jd_entertainment
    - timer: # 女装盲盒 至2021-2-25
        parameters:
          name: jd_nzmh
          cronExpression: "0 7 19-25 2 *"
          enable: true
          argument: jd_nzmh
    - timer: # 美丽研究院
        parameters:
          name: jd_beauty
          cronExpression: "1 7,12,19 * * *"
          enable: true
          argument: jd_beauty
    # - timer: # 半点红包雨
    #     parameters:
    #       name: jd_live_redrain_half
    #       cronExpression: "30,31 12-23/1 * * *"
    #       enable: true
    #       argument: jd_live_redrain_half
    # - timer: # 年货直播红包雨
    #     parameters:
    #       name: jd_live_redrain_nian
    #       cronExpression: "1 0,9-23/2 3,5,20-30/1 1,2 *"
    #       enable: true
    #       argument: jd_live_redrain_nian
    # - timer: # 官方号直播红包雨
    #     parameters:
    #       name: red_rain
    #       cronExpression: "0,1,50 0,9-23/2 8-18 2 *"
    #       enable: true
    #       argument: red_rain
    - timer: #环球挑战赛 活动时间：2021-02-02 至 2021-02-22
        parameters:
          name: jd_global
          cronExpression: "0 9,12,20,21 1-22 2 *"
          enable: true
          argument: jd_global
    # - timer: # 临时运行用#京东神仙书院答题
    #     parameters:
    #       name: temp  
    #       cronExpression: "5 8 * * *"
    #       enable: true
    #       argument: jd_immortal_answer
  environment: #  环境变量
    variables: #  环境变量对象
      AAA: BBB # 不要删除，用来格式化对齐追加的变量的
